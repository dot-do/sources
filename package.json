{
  "name": "sources.do",
  "version": "0.1.0",
  "description": "Data source definitions with Zod schema validation and database sync",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "velite build",
    "dev": "velite build --watch",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "velite": "^0.1.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
